# Audit schema

Use `schema_version: "1.0"`, `product`, `audience`, `journey`, and `findings`.

Each finding requires `id`, `dimension`, `state`, `severity`, `evidence`, `consequence`, `recommendation`, and `acceptance_criteria`. Dimension must match the rubric; severity is `critical`, `high`, `medium`, or `low`.
